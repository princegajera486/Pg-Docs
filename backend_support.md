# Backend Technical Design Document (TDD) - PG Management System

## TECHNOLOGY STACK
- Python 3.10+
- FastAPI
- Firebase Realtime Database
- Firebase Storage
- Firebase Admin SDK
- JWT Authentication
- Pydantic Validation
- Service Layer Architecture

---

## MODULE 1: AUTHENTICATION

### 1. Module Overview
The Authentication module handles admin login, session management, and JWT generation for securing all subsequent API requests.

### 2. Firebase Database Structure
- **Node Name**: `admins`
- **Purpose**: Stores administrator credentials.
- **Child Nodes**: `{admin_id}`
- **Data Types**: String, Boolean
- **Required Fields**: `email`, `password_hash`, `is_active`
- **Optional Fields**: `last_login`
- **Firebase Key Structure**: Push ID generated by Firebase (`-N...`) or deterministic string.
- **Sample JSON**:
```json
{
  "admins": {
    "admin_1": {
      "email": "admin@pg.com",
      "password_hash": "$2b$12$e/...",
      "is_active": true,
      "last_login": "2023-10-01T10:00:00Z"
    }
  }
}
```

### 3. Firebase Relationships
- Standalone node. Admin IDs may be referenced in logs or audit nodes, but are functionally independent for authentication.

### 4. FastAPI Endpoints
| Method | Endpoint | Description | Authentication |
| :--- | :--- | :--- | :--- |
| POST | `/api/v1/auth/login` | Admin login | No |
| POST | `/api/v1/auth/refresh` | Refresh access token | Yes |
| GET | `/api/v1/auth/me` | Get current admin profile | Yes |
| PUT | `/api/v1/auth/me` | Update profile (Edit API) | Yes |

### 5. Request Payload
**POST `/api/v1/auth/login` (Create API)**
```json
{
  "email": "admin@pg.com",
  "password": "SecurePassword123!"
}
```
**PUT `/api/v1/auth/me` (Edit API)**
```json
{
  "email": "newadmin@pg.com"
}
```

### 6. Response Payload
**Success (200 OK)**
```json
{
  "access_token": "eyJhbGci...",
  "refresh_token": "eyJhbGci...",
  "token_type": "bearer"
}
```
**Error (401 Unauthorized)**
```json
{
  "detail": "Invalid credentials"
}
```

### 7. Backend Validation Rules
- **Required validation**: `email` and `password` are mandatory.
- **Business validation**: Check `is_active == true`.
- **Firebase validation**: Admin node lookup by email.
- **Duplicate validation**: N/A for single admin flow.

### 8. Business Logic
Receive Credentials -> Validate Email Format -> Query Firebase `admins` node by email -> Verify bcrypt password hash -> Check `is_active` -> Generate JWT Access Token -> Return Success.

---

## MODULE 2: PG MANAGEMENT

### 1. Module Overview
Manages the properties, rooms, and bed configurations across multiple PG locations.

### 2. Firebase Database Structure
- **Node Name**: `pg_properties`
- **Purpose**: Stores details of all PG buildings.
- **Child Nodes**: `{pg_id}`, `rooms`, `beds`
- **Data Types**: String, Integer, Boolean
- **Required Fields**: `name`, `address`, `total_rooms`, `status`
- **Optional Fields**: `facilities`
- **Firebase Key Structure**: Firebase auto-generated Push IDs (e.g., `-Nabc123...`)
- **Sample JSON**:
```json
{
  "pg_properties": {
    "-N_PG123": {
      "name": "Sunrise PG",
      "address": "Block A, Tech Park",
      "total_rooms": 10,
      "status": "Active",
      "rooms": {
        "-N_RM456": {
          "room_number": "101",
          "capacity": 2,
          "beds": {
            "-N_BD789": {
              "bed_number": "B1",
              "is_occupied": false
            }
          }
        }
      }
    }
  }
}
```

### 3. Firebase Relationships
- Nested structure: `pg_properties` -> `rooms` -> `beds`.
- Bed IDs are referenced by the `Members` node to indicate occupancy.
- Real-time updates: Changing a bed's `is_occupied` to `true` reflects immediately in Dashboard aggregations.

### 4. FastAPI Endpoints
| Method | Endpoint | Description | Authentication |
| :--- | :--- | :--- | :--- |
| GET | `/api/v1/pg` | List all PGs | Yes |
| POST | `/api/v1/pg` | Create new PG | Yes |
| GET | `/api/v1/pg/{id}` | Get PG details | Yes |
| PUT | `/api/v1/pg/{id}` | Update PG (Edit API) | Yes |
| PATCH | `/api/v1/pg/{id}/status` | Change PG status | Yes |
| DELETE | `/api/v1/pg/{id}` | Delete PG | Yes |

### 5. Request Payload
**POST `/api/v1/pg` (Create API)**
```json
{
  "name": "Sunrise PG",
  "address": "Block A",
  "total_rooms": 10,
  "status": "Active"
}
```
**PUT `/api/v1/pg/{id}` (Edit API)**
```json
{
  "name": "Sunrise PG Updated",
  "address": "Block B",
  "total_rooms": 12
}
```
**PATCH `/api/v1/pg/{id}/status` (Status Update API)**
```json
{
  "status": "Inactive"
}
```

### 6. Response Payload
**Success (201 Created)**
```json
{
  "id": "-N_PG123",
  "message": "PG Created Successfully"
}
```
**Error (409 Conflict)**
```json
{
  "detail": "PG name already exists"
}
```

### 7. Backend Validation Rules
- **Required validation**: `name`, `address` required.
- **Duplicate validation**: Ensure PG name doesn't exist.
- **Business validation**: Cannot delete a PG if it has occupied beds.
- **Firebase validation**: Verify valid Firebase Key for PUT/PATCH/DELETE.

### 8. Business Logic
Receive Payload -> Validate fields -> Check Duplicate Name -> Create PG node in `pg_properties` -> Return Success.

---

## MODULE 3: MEMBER MANAGEMENT

### 1. Module Overview
Handles tenant lifecycle, including KYC document links, bed allocation, and rent mapping.

### 2. Firebase Database Structure
- **Node Name**: `members`
- **Purpose**: Store tenant profiles.
- **Child Nodes**: `{member_id}`
- **Data Types**: String, Integer
- **Required Fields**: `name`, `mobile`, `pg_id`, `room_id`, `bed_id`, `rent_amount`, `status`
- **Optional Fields**: `kyc_urls`
- **Firebase Key Structure**: Firebase auto-generated Push IDs.
- **Sample JSON**:
```json
{
  "members": {
    "-N_MEM001": {
      "name": "John Doe",
      "mobile": "9876543210",
      "pg_id": "-N_PG123",
      "room_id": "-N_RM456",
      "bed_id": "-N_BD789",
      "rent_amount": 8000,
      "status": "Active",
      "joining_date": "2023-10-01"
    }
  }
}
```

### 3. Firebase Relationships
- `Member` -> `PG/Room/Bed`: Stores the IDs to reference the allocated bed.
- `Member` -> `Rent Details`: Referenced in the `rent_records` node via `member_id`.
- Requires transactional update when assigning a bed (Member node created + Bed node updated to `is_occupied: true`).

### 4. FastAPI Endpoints
| Method | Endpoint | Description | Authentication |
| :--- | :--- | :--- | :--- |
| GET | `/api/v1/members` | List members | Yes |
| POST | `/api/v1/members` | Onboard member | Yes |
| GET | `/api/v1/members/{id}` | Get member details | Yes |
| PUT | `/api/v1/members/{id}` | Update member (Edit API) | Yes |
| PATCH | `/api/v1/members/{id}/status` | Update member status | Yes |
| DELETE | `/api/v1/members/{id}` | Delete member | Yes |

### 5. Request Payload
**POST `/api/v1/members` (Create API)**
```json
{
  "name": "John Doe",
  "mobile": "9876543210",
  "pg_id": "-N_PG123",
  "room_id": "-N_RM456",
  "bed_id": "-N_BD789",
  "rent_amount": 8000
}
```
**PUT `/api/v1/members/{id}` (Edit API)**
```json
{
  "name": "John Doe",
  "mobile": "9876543210",
  "rent_amount": 8500
}
```
**PATCH `/api/v1/members/{id}/status` (Status Update API)**
```json
{
  "status": "Inactive"
}
```

### 6. Response Payload
**Success (201 Created)**
```json
{
  "id": "-N_MEM001",
  "message": "Member onboarded successfully"
}
```
**Error (422 Unprocessable Entity)**
```json
{
  "detail": "Bed is already occupied"
}
```

### 7. Backend Validation Rules
- **Required validation**: All identity and allocation fields.
- **Duplicate validation**: Mobile number must be unique.
- **Business validation**: Ensure selected bed `is_occupied == false`.
- **Relationship validation**: `pg_id`, `room_id`, and `bed_id` must exist.
- **Firebase validation**: Concurrent modification check using Firebase transactions.

### 8. Business Logic
Member Registration -> Validate PG -> Validate Room -> Validate Bed -> Begin Transaction -> Save Member in `members` -> Update Bed `is_occupied=true` -> Generate Payment Record in `rent_records` -> Commit Transaction -> Return Success.

---

## MODULE 4: PAYMENT VERIFICATION

### 1. Module Overview
Manages the generation of rent dues, processing of member-submitted payment screenshots, and admin verification workflow.

### 2. Firebase Database Structure
- **Node Name**: `rent_records`
- **Purpose**: Stores monthly rent obligations and payment status.
- **Child Nodes**: `{rent_id}`
- **Data Types**: String, Integer
- **Required Fields**: `member_id`, `amount`, `due_date`, `status`
- **Optional Fields**: `screenshot_url`, `transaction_id`, `verified_by`
- **Firebase Key Structure**: Firebase auto-generated Push IDs.
- **Sample JSON**:
```json
{
  "rent_records": {
    "-N_RNT001": {
      "member_id": "-N_MEM001",
      "amount": 8000,
      "due_date": "2023-11-05",
      "status": "In Review",
      "screenshot_url": "https://storage.googleapis.com/.../receipt.jpg",
      "submitted_at": "2023-11-04T12:00:00Z"
    }
  }
}
```

### 3. Firebase Relationships
- `Payment` -> `Member`: Links back to tenant via `member_id`.
- `Payment` -> `Statement Verification`: Bank statement IDs are mapped here if auto-verified via PDF upload.

### 4. FastAPI Endpoints
| Method | Endpoint | Description | Authentication |
| :--- | :--- | :--- | :--- |
| GET | `/api/v1/payments` | List rent records | Yes |
| POST | `/api/v1/payments/submit` | Submit payment (Member) | No (Secure Link) |
| GET | `/api/v1/payments/{id}` | Get rent details | Yes |
| PUT | `/api/v1/payments/{id}` | Edit rent record | Yes |
| PATCH | `/api/v1/payments/{id}/verify` | Admin verify payment | Yes |

### 5. Request Payload
**POST `/api/v1/payments/submit` (Create API for member)**
```json
{
  "transaction_id": "UPI123456789",
  "screenshot_url": "https://storage..."
}
```
**PUT `/api/v1/payments/{id}` (Edit API)**
```json
{
  "amount": 8500,
  "due_date": "2023-11-10"
}
```
**PATCH `/api/v1/payments/{id}/verify` (Update API)**
```json
{
  "status": "Paid",
  "remarks": "Verified successfully"
}
```

### 6. Response Payload
**Success (200 OK)**
```json
{
  "id": "-N_RNT001",
  "status": "Paid"
}
```
**Error (400 Bad Request)**
```json
{
  "detail": "Record is not in review state"
}
```

### 7. Backend Validation Rules
- **Business validation**: Cannot verify a payment not marked 'In Review'.
- **Image validation**: Ensure `screenshot_url` exists if manually verified via image.

### 8. Business Logic
Receive Payment Verification -> Fetch rent record -> Validate current status -> If Approved: Update status to 'Paid' -> If Rejected: Update status to 'Rejected' -> Add Remarks -> Trigger Notification (if applicable) -> Return Success.

---

## MODULE 5: BANK STATEMENT UPLOAD

### 1. Module Overview
Handles parsing of bank statement PDFs to auto-match and verify pending payments.

### 2. Firebase Database Structure
- **Node Name**: `statements`
- **Purpose**: Stores upload history and processing status.
- **Child Nodes**: `{statement_id}`
- **Data Types**: String, Integer
- **Required Fields**: `file_url`, `upload_date`, `status`
- **Optional Fields**: `matched_count`, `unmatched_count`
- **Firebase Key Structure**: Firebase auto-generated Push IDs.
- **Sample JSON**:
```json
{
  "statements": {
    "-N_STMT123": {
      "file_url": "https://storage...",
      "upload_date": "2023-11-06T10:00:00Z",
      "status": "Processed",
      "matched_count": 15,
      "unmatched_count": 2
    }
  }
}
```

### 3. Firebase Relationships
- `Statement` -> `Rent Records`: Processed statements update the status of matched `rent_records` in batch.

### 4. FastAPI Endpoints
| Method | Endpoint | Description | Authentication |
| :--- | :--- | :--- | :--- |
| POST | `/api/v1/statements/upload` | Upload bank PDF | Yes |
| GET | `/api/v1/statements` | List uploaded statements| Yes |
| GET | `/api/v1/statements/{id}` | Get statement info | Yes |
| PUT | `/api/v1/statements/{id}` | Edit statement record | Yes |
| PATCH | `/api/v1/statements/{id}/process`| Trigger processing | Yes |

### 5. Request Payload
**POST `/api/v1/statements/upload` (Statement Upload API)**
- **Content-Type**: `multipart/form-data`
- **Form Data**: `file` (PDF), `password` (String - Optional)

**PUT `/api/v1/statements/{id}` (Edit API)**
```json
{
  "status": "Archived"
}
```
**PATCH `/api/v1/statements/{id}/process` (Update API)**
```json
{
  "action": "retry"
}
```

### 6. Response Payload
**Success (201 Created)**
```json
{
  "id": "-N_STMT123",
  "status": "Processing Started"
}
```
**Error (422 Unprocessable Entity)**
```json
{
  "detail": "Invalid PDF format or wrong password"
}
```

### 7. Backend Validation Rules
- **File validation**: Must be PDF format. Max size 10MB.
- **Statement validation**: Requires PDF password if encrypted.
- **Business validation**: Prevent re-processing already processed statements.

### 8. Business Logic
Admin Uploads Bank Statement PDF -> Validate PDF -> Validate Password -> Save to Firebase Storage -> Create record in `statements` -> Extract Transactions -> Read Transaction Details -> Match Transactions with Pending Member Payments -> Update Matching Records -> Mark Payment Status -> Generate Processing Summary -> Update statement record status -> Return Success.

---

## MODULE 6: DASHBOARD

### 1. Module Overview
Aggregates key metrics for the admin interface, requiring high-performance reads from Firebase.

### 2. Firebase Database Structure
- No separate core tables. Uses Firebase aggregations or a cached `dashboard_stats` node.
- **Node Name**: `dashboard_cache` (Updated via background jobs)
- **Sample JSON**:
```json
{
  "dashboard_cache": {
    "total_active_members": 45,
    "total_vacant_beds": 5,
    "pending_verifications": 12,
    "last_updated": "2023-11-06T10:00:00Z"
  }
}
```

### 3. Firebase Relationships
- Aggregates data from `members`, `pg_properties`, and `rent_records`.

### 4. FastAPI Endpoints
| Method | Endpoint | Description | Authentication |
| :--- | :--- | :--- | :--- |
| GET | `/api/v1/dashboard/kpis` | Get top KPIs | Yes |
| GET | `/api/v1/dashboard/charts` | Get revenue chart data | Yes |
| GET | `/api/v1/dashboard/tables` | Get recent transactions | Yes |
| GET | `/api/v1/dashboard/alerts` | Get pending actions | Yes |

### 5. Request Payload
(All GET requests, no request payload)

### 6. Response Payload
**Success (200 OK)**
```json
{
  "kpis": {
    "total_members": 45,
    "revenue": 360000,
    "pending_dues": 12000
  }
}
```

### 7. Backend Validation Rules
- Ensure the user is authenticated with a valid JWT.

### 8. Business Logic
Receive Request -> Read `dashboard_cache` node (for fast response) -> If cache expired, trigger background refresh -> Return JSON -> Return Success.

---

## 9. Service Layer
The architecture uses Service Layer classes to separate business logic from routing.
- **AuthenticationService**: Handles JWT creation, payload decoding, and password hashing.
- **DashboardService**: Fetches and aggregates KPI data. Uses Firebase queries to count metrics.
- **PGService**: Manages the hierarchy of PGs, Rooms, and Beds.
- **MemberService**: Handles onboarding, profile updates, and triggering bed state changes.
- **PaymentService**: Handles manual verification logic, checking payment statuses, and updating rent records.
- **StatementService**: Uses PDF parsing libraries, implements matching algorithms, and updates records.
- **NotificationService**: Generates WhatsApp links and pushes notifications.
- **FirebaseService**: Centralized service wrapping the Firebase Admin SDK for DB and Storage interactions.

---

## 10. Firebase Operations
Firebase Realtime Database interactions:
- **Create**: Using `.push()` equivalent in Admin SDK to auto-generate unique keys.
- **Read**: Using `.get()` for single fetch, `.order_by_child()` for filtering (e.g., fetching active members).
- **Update**: Using `.update()` for modifying specific child nodes without overwriting.
- **Delete**: Using `.delete()` to remove nodes.
- **Batch Update**: Sending a dictionary of multiple paths to `.update()` on the root reference for atomic multi-location updates.
- **Transaction**: Using Firebase transaction blocks to handle concurrent bed allocations.
- **Real-time Listener**: Backend generally uses REST/Admin SDK fetching, but workers can use listeners for queue processing.
- **Atomic Update**: Utilizing path-based updates (`members/-N123/status: Inactive`).
- **Offline Synchronization**: Mostly relevant for frontend SDKs, backend uses Admin SDK which operates online.

---

## 11. Authentication
- **JWT Authentication**: Secure API endpoints via stateless JSON Web Tokens.
- **Login Flow**: Admin provides email/password -> Backend verifies against Firebase `admins` node -> Returns JWT.
- **Access Token**: Short-lived (e.g., 15 mins).
- **Refresh Token**: Long-lived (e.g., 7 days) to get new access tokens.
- **Password Hashing**: `bcrypt` ensures raw passwords are never stored.
- **Session Expiration**: Enforced by JWT expiration claim (`exp`).
- **Single Admin Login**: System designed for a single admin/owner structure.

---

## 12. Exception Handling
FastAPI Exception Handlers to return standardized JSON responses.
- **ValidationException** (422): Pydantic input validation failures.
- **FirebaseException** (500): Database connectivity or SDK errors.
- **AuthenticationException** (401): Invalid or expired JWT.
- **DuplicateException** (409): Resource already exists (e.g., Mobile number).
- **BusinessException** (400): Domain logic violations (e.g., Bed already occupied).
- **FileUploadException** (400): Invalid file format/size.
- **StatementProcessingException** (422): Failure to parse PDF or wrong password.
- **PaymentException** (400): Invalid payment transition state.
- **NetworkException** (503): Third-party API failures.

---

## 13. Logging
- **Authentication Logs**: Failed/successful login attempts.
- **API Logs**: Request/Response paths, timings, HTTP status codes.
- **Firebase Logs**: Database write latency and transaction failures.
- **Statement Upload Logs**: Tracing the PDF parsing pipeline and match statistics.
- **Payment Logs**: Audit trails of who verified a payment and when.
- **Exception Logs**: Stack traces captured and formatted.
- **System Logs**: Worker startup, cron job execution status.

---

## 14. File Storage
Using **Firebase Storage**.
- **Folders**:
  - `kyc/aadhaar/`
  - `kyc/pan/`
  - `kyc/dl/`
  - `payments/screenshots/`
  - `bank_statements/pdfs/`
- **Naming Convention**: Use UUID for filenames (`{uuid4()}.pdf`) to prevent overriding and URL guessing.
- **Access URLs**: Use Firebase Storage pre-signed URLs (Signed URLs via Admin SDK) with expiration for secure access, instead of public URLs.
- **File Security**: Only authenticated API routes can generate Signed URLs to access these files.

---

## 15. Dashboard APIs
- **KPI Cards**: `/api/v1/dashboard/kpis`
- **Charts**: `/api/v1/dashboard/charts`
- **Tables**: `/api/v1/dashboard/tables`
- **Alerts**: `/api/v1/dashboard/alerts`
- **Queries**: Due to NoSQL nature, backend utilizes `.order_by_child('status').equal_to('In Review')` and caches heavy aggregation queries in the `dashboard_cache` node.

---

## 16. Payment Verification Flow
1. **Rent Due**: Background job creates `Pending` record in `rent_records`.
2. **WhatsApp Link Generated**: Notification service creates a secure JWT-based payment link.
3. **Member Opens Link**: Member sees UI with due amount.
4. **Select Payment Method**: Chooses UPI.
5. **UPI App Opens**: Member pays via intent link.
6. **Payment Completed**: Member uploads screenshot.
7. **Screenshot Uploaded**: Saved to Firebase Storage, URL attached to rent record.
8. **Status becomes "In Review"**: `rent_records` node updated.
9. **Admin Verification**: Admin reviews on dashboard.
10. **Approved -> "Paid"**: Record updated. OR **Rejected -> "Rejected"**: Record updated, member notified to retry.

---

## 17. Bank Statement Upload Flow
1. **Admin Uploads Bank Statement PDF**: API receives file.
2. **Validate PDF**: Check mime type.
3. **Validate Password**: Use password if provided for protected PDFs.
4. **Extract Transactions**: Use PyMuPDF/pdfplumber to read tabular text.
5. **Read Transaction Details**: Parse Date, Amount, Remarks/UPI ID.
6. **Match Transactions with Pending Member Payments**: Logic matches amount and fuzzy-matches remarks with tenant name or mapped UPI IDs.
7. **Update Matching Records**: Prepare Firebase Batch Update.
8. **Mark Payment Status**: Change matched rents to 'Paid'.
9. **Generate Processing Summary**: Store results in `statements` node (Success count, Unmatched count).

---

## 18. Scheduled Background Jobs
Using `FastAPI BackgroundTasks` or `Celery`:
- **Daily Due Reminder**: Send WhatsApp msg for pending rents on due date.
- **48-Hour Reminder**: Escalate if unpaid after 48 hours.
- **Payment Reminder**: General monthly broadcast.
- **Dashboard Cache Refresh**: Hourly recalculation of KPIs to save Firebase reads.
- **Statement Processing Queue**: Async processing of large PDFs.
- **Expired Link Cleanup**: Invalidate old JWT payment links.
- **Notification Queue**: Ensure non-blocking WhatsApp dispatches.

---

## 19. Notifications
- **WhatsApp Notification**: Primary channel via Cloud API. Used for Payment Links, Verifications, Reminders.
- **Future Email Notification**: For sending admin monthly reports.
- **Future SMS Notification**: Fallback for urgent alerts.
- **Events Triggers**:
  - Payment Reminder
  - Rent Due Reminder
  - Payment Verification
  - Statement Processing Completion

---

## 20. Security
- **JWT Security**: Signed with HS256, verified via FastAPI dependencies.
- **Password Hashing**: `bcrypt` ensures raw passwords are never stored.
- **Firebase Security Rules**: DB locked down (`".read": false, ".write": false`) since all interaction happens via backend Admin SDK.
- **Input Validation**: Pydantic strictly checks types and formats.
- **API Rate Limiting**: Limit `/login` and `/upload` endpoints to prevent brute force.
- **File Upload Security**: Validate extensions and magic bytes. Enforce size limits.
- **Secure File Access**: Pre-signed URLs expire after a set time.
- **HTTPS Enforcement**: Standard on production servers.
- **CORS Configuration**: Allowed origins restricted to the known frontend URL.
- **Request Validation**: Reject extra payload fields.

---

## 21. Project Folder Structure
```text
app/
├── api/             # FastAPI routers mapping HTTP methods to Service layer
├── core/            # Configs, JWT settings, Security dependencies
├── firebase/        # Firebase Admin SDK initialization and FirebaseService
├── models/          # Data structures
├── schemas/         # Pydantic models for validation (Requests/Responses)
├── services/        # Core Business Logic Layer (MemberService, PaymentService)
├── middleware/      # CORS, Logging, Exception Handlers
├── validators/      # Custom Pydantic field validators
├── exceptions/      # Custom exception classes
├── tasks/           # Background jobs and scheduler setup
├── uploads/         # Temporary storage during PDF parsing
├── utils/           # Helper functions (hashing, matching algorithms)
└── tests/           # Unit and integration tests
```
- **api/**: Defines Endpoints.
- **services/**: Contains the logic (e.g. `member_service.py`).
- **firebase/**: Abstracts the Admin SDK interactions.
- **schemas/**: Houses Pydantic Request/Response classes.
- **tasks/**: Contains all Cron routines.
